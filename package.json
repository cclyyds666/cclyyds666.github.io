{
  "name": "cclyyds666-personal-site",
  "version": "1.0.0",
  "description": "Personal website with an Express API and SQLite persistence.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "vitest run tests/api.test.js"
  },
  "engines": {
    "node": ">=24.0.0 <25.0.0"
  },
  "dependencies": {
    "express": "^5.1.0",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "supertest": "^7.1.4",
    "vitest": "^4.0.13"
  }
}
